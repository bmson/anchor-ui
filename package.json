{
  "name": "anchor-ai",
  "version": "0.0.0",
  "license": "MIT",
  "bin": "dist/cli.js",
  "type": "module",
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "build": "node esbuild.config.js",
    "dev": "node esbuild.config.js --watch",
    "link": "npm run build && npm link",
    "unlink": "npm unlink -g anchor-ai",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "ink": "^5.2.1",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
